<h1><%= t('.header') %></h1>

<% if @gists.present? %>
  <table class="table table-hover">
    <tr>
      <th><%= t('.question') %></a></th>
      <th><%= t('.url') %></th>
      <th><%= t('.author') %></th>
    </tr>
    <%= render @gists %>
  </table>
<% else %>
  <h1><%= t('.no_gists') %></h1>
<% end %>
