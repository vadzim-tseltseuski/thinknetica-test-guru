<div class="row justify-content-center mt-3">
  <div class="col col-md-12">
    <%= @badge.title %>
    <hr>

    <p>
      <img class="img-fluid img-thumbnail" src="<%= @badge.image_url.html_safe %>">
    </p>

    <%= link_to t('.back'), admin_badges_path %>
  </div>
</div>
